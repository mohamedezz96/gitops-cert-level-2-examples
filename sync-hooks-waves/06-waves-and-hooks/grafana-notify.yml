apiVersion: batch/v1
kind: Job
metadata:
  namespace: example06
  generateName: grafana-notify-
  labels:
    codefresh.io/cert: grafana-job  
  Annotations:
    argocd.argoproj.io/sync-wave: sync
    argocd.argoproj.io/hook: -10
spec:
  template:
    spec:
      containers:
      - name: dummy-run
        image: alpine
        command:
          - "sleep"
          - "10"
      restartPolicy: Never
  backoffLimit: 2    
